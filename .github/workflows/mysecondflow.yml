name: 2ndworkflow
on:
  workflow_dispatch:
jobs:
  Codecheckout:
    runs-on: ubuntu-latest
    steps:
      - name: Git code checkout
        run: echo "code is checked out"
        
      - name: Code checkout from
  Build:
    runs-on: ubuntu-latest
    needs: codecheckout
    steps:
      - name: Build the code
        run: echo "code is built"
  Test:
    runs-on: ubuntu-latest
    needs: 
      - codecheckout
      - build
    steps:
      - name: Test the code
        run: echo "code is tested"
